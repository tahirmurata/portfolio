---
import {
    Card,
    CardContent,
    CardDescription,
    CardFooter,
    CardHeader,
    CardTitle,
} from "@/components/ui/card";
---

<Card className="md:row-span-5 lg:row-span-4 md:row-start-3 lg:row-start-5">
    <CardHeader>
        <CardTitle>Contact me</CardTitle>
    </CardHeader>
    <CardContent>
        <p class="text-muted-foreground italic">Email</p>
        <ul class="my-2 [&>li]:mt-1">
            <li id="c921587">
                <span class="text-muted-foreground">Enable Javascript</span>
            </li>
        </ul>
        <p class="text-muted-foreground italic">Socials</p>
        <ul class="my-2 [&>li]:mt-1">
            <li>Github</li>
            <li>Gitlab</li>
            <li>Twitter</li>
        </ul>
    </CardContent>
</Card>

<script>
    import { xchacha20poly1305 } from "@noble/ciphers/chacha";
    import { managedNonce } from "@noble/ciphers/webcrypto";
    import { hexToBytes, bytesToUtf8, utf8ToBytes } from "@noble/ciphers/utils";
    const key = hexToBytes(
        "1006ee0113156d1a1d0b9843a271df3bbb20d387cf4a1dbb5a96a43e1311d220",
    );
    const chacha = managedNonce(xchacha20poly1305)(key); // manages nonces for you
    // const data = utf8ToBytes("email@example.com");
    // const ciphertext = chacha.encrypt(data);
    // navigator.clipboard.writeText(ciphertext.toString());
    // console.log(ciphertext);
    const ciphertext = new Uint8Array([
        105, 76, 51, 241, 70, 69, 99, 42, 47, 94, 70, 191, 64, 229, 235, 190,
        164, 3, 243, 53, 241, 164, 231, 56, 45, 189, 141, 218, 22, 105, 238, 30,
        219, 102, 64, 222, 76, 243, 59, 74, 130, 26, 77, 245, 93, 93, 190, 241,
        198, 210, 122, 201, 108, 70, 48, 93, 10,
    ]);
    const data_ = chacha.decrypt(ciphertext);

    document.querySelector("#c921587")!.textContent = bytesToUtf8(data_);
</script>
